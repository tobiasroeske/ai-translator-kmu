# Dependencies (also ignored by Prettier's defaults, listed for clarity)
node_modules

# agents
.agents

# local tooling config (Claude Code)
.claude

# Lockfiles — never reformat, it causes churn and can break resolution
pnpm-lock.yaml

# Build output / generated artifacts
.next
out
build
coverage

# written by the validate:* scripts — generated per run, not hand-maintained
docs/validation

# Logs
*.log

# shadcn/ui — vendored/generated via CLI, not hand-maintained
components/ui

# written by the Supabase CLI on `supabase start` — bundled runtime code
supabase/.temp
